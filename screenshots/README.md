The screenshots in this directory are updated manually on a best-effort basis.

To generate them, run:

   pip install seltest

   sel -b chrome update screenshots

You'll need to install chrome webdriver for this to work.
